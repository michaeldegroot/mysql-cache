{
  "name": "mysql-cache",
  "version": "0.5.6",
  "description": "Automatically caches SELECT sql's in memory",
  "main": "app.js",
  "scripts": {
    "test": "mocha",
    "cover": "istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec ./test/*",
    "coveralls": "npm bin /istanbul cover ./node_modules/mocha/bin/_mocha -- -R spec ./test/* && node node_modules/coveralls/bin/coveralls.js < coverage/lcov.info"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:michaeldegroot/mysql-cache.git"
  },
  "keywords": [
    "mysql",
    "cache",
    "mysql-cache",
    "driver"
  ],
  "author": {
    "name": "michaeldegroot",
    "email": "specamps@gmail.com"
  },
  "license": "MIT",
  "dependencies": {
    "colors": "^1.1.2",
    "console-debug": "^0.4.2",
    "mysql": "^2.9.0",
    "node-cache": "^3.0.0",
    "optimist": "^0.6.1"
  },
  "devDependencies": {
    "assert": "^1.3.0",
    "assert-plus": "^1.0.0",
    "coveralls": "^2.11.8",
    "express": "^4.13.3",
    "istanbul": "^0.4.1",
    "mocha": "^3.0.2",
    "mocha-lcov-reporter": "^1.0.0"
  }
}
